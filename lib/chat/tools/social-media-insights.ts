export async function runSocialMediaInsights(input: {
    cruiseLine: string;
    shipName: string | null;
    destination: string | null;
}): Promise<never> {
    throw new Error(
        `Not implemented: runSocialMediaInsights (${input.cruiseLine}).`
    );
}
