import { reviseDiscoveryBlueprint } from '../lib/campaigns/discovery-revision'; const slug = process.argv[2]; if (!slug) { console.error('Usage: npx tsx scripts/revise-campaign.ts <slug>'); process.exit(1); } reviseDiscoveryBlueprint(slug).then(res => { console.log('SUCCESS:', res.campaign.id); process.exit(0); }).catch(e => { console.error('FAILED:', e.message); process.exit(1); });
