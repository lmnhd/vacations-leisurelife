import { reviseDiscoveryBlueprint } from '../lib/campaigns/discovery-revision'; const slugs = ['aesthetic-scandinavia-2026', 'sea-sillage-indie-fragrance-social', 'bookish-mediterranean-2026']; async function run() { for (const slug of slugs) { console.log('Revising:', slug); try { const res = await reviseDiscoveryBlueprint(slug); console.log('Revised:', res.campaign.id); } catch(e) { console.error('Error on', slug, e.message); } } } run();
