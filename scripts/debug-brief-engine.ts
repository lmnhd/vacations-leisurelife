import { getCampaignBlueprint } from '../lib/campaigns/campaign-store'; import { createOrRefreshBrief } from '../lib/campaigns/brief-engine/orchestrator'; async function run() { try { console.log('Starting...'); const res = await createOrRefreshBrief('night-sky-sea-2026'); console.log(JSON.stringify(res.issues, null, 2)); } catch(e) { console.error('Caught:', e.message); } } run();
